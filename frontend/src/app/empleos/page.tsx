import type {Metadata} from "next"; import Link from "next/link"; import {JobCard} from "@/components/job-card"; import {categories,jobs} from "@/data/jobs";
export const metadata:Metadata={title:"Buscar empleos"}; type Search={q?:string;location?:string;category?:string;modality?:string;sort?:string};
export default async function JobsPage({searchParams}:{searchParams:Promise<Search>}){const p=await searchParams;const q=p.q?.trim().toLocaleLowerCase("es")??"";let found=jobs.filter(j=>{const text=`${j.title} ${j.company} ${j.category} ${j.summary}`.toLocaleLowerCase("es");return(!q||text.includes(q))&&(!p.location||j.location===p.location)&&(!p.category||j.category===p.category)&&(!p.modality||j.modality===p.modality)});if(p.sort==="salary")found=[...found].sort((a,b)=>b.salaryFrom-a.salaryFrom);return <main className="min-h-[70vh] bg-slate-50"><section className="border-b bg-white"><div className="mx-auto max-w-6xl px-5 py-10"><h1 className="text-3xl font-black">Buscar empleos</h1><form className="mt-6 grid gap-3 md:grid-cols-[1fr_.7fr_auto]"><input name="q" defaultValue={p.q} className="rounded-xl border border-slate-300 px-4 py-3.5" placeholder="Cargo, empresa o palabra clave"/><select name="location" defaultValue={p.location} className="rounded-xl border border-slate-300 px-4"><option value="">Todo el país</option><option>Distrito Nacional</option><option>Santo Domingo</option><option>Santiago</option><option>República Dominicana</option></select><button className="rounded-xl bg-blue-700 px-6 font-bold text-white">Buscar</button></form></div></section><div className="mx-auto grid max-w-6xl gap-7 px-5 py-10 lg:grid-cols-[240px_1fr]"><aside><form className="sticky top-24 space-y-5 rounded-2xl border bg-white p-5"><input type="hidden" name="q" value={p.q??""}/><h2 className="font-extrabold">Filtros</h2><label className="block text-sm font-bold">Área<select name="category" defaultValue={p.category} className="mt-2 w-full rounded-lg border p-3 font-normal"><option value="">Todas</option>{categories.map(c=><option key={c}>{c}</option>)}</select></label><label className="block text-sm font-bold">Modalidad<select name="modality" defaultValue={p.modality} className="mt-2 w-full rounded-lg border p-3 font-normal"><option value="">Todas</option><option>Presencial</option><option>Remoto</option><option>Híbrido</option></select></label><label className="block text-sm font-bold">Orden<select name="sort" defaultValue={p.sort} className="mt-2 w-full rounded-lg border p-3 font-normal"><option value="">Más recientes</option><option value="salary">Mejor salario</option></select></label><button className="w-full rounded-lg bg-slate-900 p-3 text-sm font-bold text-white">Aplicar filtros</button></form></aside><section><p className="mb-5 font-bold text-slate-700">{found.length} {found.length===1?"vacante encontrada":"vacantes encontradas"}</p>{found.length?<div className="grid gap-5">{found.map(job=><JobCard key={job.slug} job={job}/>)}</div>:<div className="rounded-2xl border border-dashed bg-white p-10 text-center"><h2 className="text-xl font-extrabold">No encontramos resultados</h2><p className="mt-2 text-slate-500">Prueba otra palabra o elimina filtros.</p><Link href="/empleos" className="mt-5 inline-block font-bold text-blue-700">Ver todas las vacantes</Link></div>}</section></div></main>}
