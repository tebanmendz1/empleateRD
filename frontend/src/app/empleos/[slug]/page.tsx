import type {Metadata} from "next";import Link from "next/link";import {notFound} from "next/navigation";import {getJob,jobs} from "@/data/jobs";
export function generateStaticParams(){return jobs.map(j=>({slug:j.slug}))} export async function generateMetadata({params}:{params:Promise<{slug:string}>}):Promise<Metadata>{const j=getJob((await params).slug);return j?{title:j.title,description:j.summary}:{}}
export default async function JobDetail({params}:{params:Promise<{slug:string}>}){const j=getJob((await params).slug);if(!j)notFound();return <main className="bg-slate-50"><div className="mx-auto max-w-6xl px-5 py-10"><Link href="/empleos" className="text-sm font-bold text-blue-700">← Volver a empleos</Link><div className="mt-6 grid gap-7 lg:grid-cols-[1fr_340px]"><article className="rounded-3xl border bg-white p-6 sm:p-9"><div className="flex gap-4"><div className="flex h-14 w-14 items-center justify-center rounded-2xl bg-blue-50 font-black text-blue-700">{j.initials}</div><div><h1 className="text-3xl font-black">{j.title}</h1><Link href={`/empresas/${j.companySlug}`} className="mt-1 block font-bold text-blue-700">{j.company}</Link></div></div><div className="mt-6 flex flex-wrap gap-2 text-sm">{[j.location,j.modality,j.contract].map(x=><span key={x} className="rounded-full bg-slate-100 px-3 py-1.5">{x}</span>)}</div><section className="mt-9 space-y-4"><h2 className="text-xl font-extrabold">Sobre la posición</h2>{j.description.map(x=><p key={x} className="leading-7 text-slate-600">{x}</p>)}<h2 className="pt-4 text-xl font-extrabold">Requisitos</h2><ul className="list-disc space-y-2 pl-5 text-slate-600">{j.requirements.map(x=><li key={x}>{x}</li>)}</ul><h2 className="pt-4 text-xl font-extrabold">Beneficios</h2><ul className="list-disc space-y-2 pl-5 text-slate-600">{j.benefits.map(x=><li key={x}>{x}</li>)}</ul></section></article><aside><div className="sticky top-24 rounded-3xl border bg-white p-6 shadow-sm"><p className="text-sm text-slate-500">Salario mensual</p><p className="mt-1 text-xl font-black">{j.salary}</p><p className="mt-4 text-sm text-slate-500">Publicada {j.postedAt.toLowerCase()}</p><Link href={`/empleos/${j.slug}/aplicar`} className="mt-6 block rounded-xl bg-emerald-500 px-5 py-4 text-center font-black">Aplicar ahora</Link><button className="mt-3 w-full rounded-xl border px-5 py-3.5 font-bold">Guardar vacante</button><p className="mt-5 text-center text-xs text-slate-500">Nunca pagues dinero para participar en un proceso.</p></div></aside></div></div></main>}
