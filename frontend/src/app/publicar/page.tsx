"use client";
import Link from "next/link";import {useRouter} from "next/navigation";import {useEffect} from "react";import {useStoredUser} from "@/components/account-nav";
export default function Publish(){const router=useRouter();const user=useStoredUser();useEffect(()=>{const token=localStorage.getItem("empleaterd_token");if(!token){router.replace("/acceso?next=/publicar");return}if(user&&user.account_type!=="company")router.replace("/panel")},[router,user]);if(!user||user.account_type!=="company")return <main className="min-h-[65vh] bg-slate-50"/>;return <main className="flex min-h-[65vh] items-center justify-center bg-slate-50 px-5"><section className="w-full max-w-xl rounded-3xl border bg-white p-8 text-center"><p className="text-sm font-bold uppercase text-blue-700">Para empresas y representantes</p><h1 className="mt-2 text-3xl font-black">Publicación inteligente</h1><p className="mt-4 text-slate-600">El cuestionario de cotización se incorporará en el bloque empresarial del MVP.</p><Link href="/panel" className="mt-6 inline-block font-bold text-blue-700">Volver al panel</Link></section></main>}
