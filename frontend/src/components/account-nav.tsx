"use client";
import Link from "next/link";import {useRouter} from "next/navigation";import {useSyncExternalStore} from "react";import {api} from "@/lib/api";
export type StoredUser={name:string;email:string;account_type:"candidate"|"company";email_verified?:boolean};
const subscribe=(callback:()=>void)=>{window.addEventListener("storage",callback);window.addEventListener("empleaterd-session",callback);return()=>{window.removeEventListener("storage",callback);window.removeEventListener("empleaterd-session",callback)}};
export function useStoredUser(){const value=useSyncExternalStore(subscribe,()=>localStorage.getItem("empleaterd_user"),()=>null);if(!value)return null;try{return JSON.parse(value) as StoredUser}catch{return null}}
export function AccountNav(){const router=useRouter();const user=useStoredUser();async function logout(){const token=localStorage.getItem("empleaterd_token");try{if(token)await api("/auth/logout",{method:"POST",headers:{Authorization:`Bearer ${token}`}})}finally{localStorage.removeItem("empleaterd_token");localStorage.removeItem("empleaterd_user");window.dispatchEvent(new Event("empleaterd-session"));router.push("/")}}if(!user)return <Link href="/acceso" className="hidden text-slate-700 sm:block">Iniciar sesión</Link>;return <div className="flex items-center gap-3"><Link href="/panel" className="max-w-32 truncate text-slate-700">{user.name}</Link><button onClick={logout} className="text-xs text-slate-500 hover:text-rose-700">Salir</button></div>}
